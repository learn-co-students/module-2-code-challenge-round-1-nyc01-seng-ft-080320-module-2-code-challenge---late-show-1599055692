<h1>Episode: <%= @episode.number %></h1>
<h3>Aired on: <%= @episode.date.strftime("%B %d, %Y") %></h3>

<% if @episode.guests.count > 0 %>
<h3>Average Rating: <%= @episode.average_rating %></h3>

<h3> Guest(s):</h3>
    <ul>
        <% @episode.guests.each do |guest| %>
            <li><%= link_to guest.name, guest_path(guest) %></li>
        <% end %>
    </ul>
<% else %>
    <p>- No Guests for this episode</p>
<% end %>

<%= link_to "Add Guest", new_appearance_path %>