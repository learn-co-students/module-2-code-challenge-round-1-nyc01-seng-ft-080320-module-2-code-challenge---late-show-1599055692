<h1>Episode Show Page</h1>

<div>Episode Date:
<h3><%= @episode.date %></h3>
</div>

Episode Guests:
<ul><% @episode.guests.each do |guest| %>
        <li><%= link_to(guest.name, guest_path(guest)) %></li>
    <% end %>
</ul>

Average Rating:
<h3><%= @episode.average_rating %></h3>