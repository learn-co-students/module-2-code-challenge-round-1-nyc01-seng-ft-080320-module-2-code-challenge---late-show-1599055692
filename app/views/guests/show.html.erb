<h1><%= @guest.name %></h1>

<h3>Appeared in:</h3>
<ul>
    <% @guest.appearances.each do |appearance| %>
        <li><%= link_to appearance.episode.date, episode_path(appearance.episode) %> <%= "Episode Number: #{appearance.episode.number} Rated: #{appearance.rating}"  %> </li>
    <% end %>
</ul>